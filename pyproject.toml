[project]
name = "comfyui-slack"
version = "0.1.0"
description = "ComfyUI custom nodes for sending images and videos to Slack"
requires-python = ">=3.9"
dependencies = [
    "slack-sdk>=3.21.0",
    "imageio-ffmpeg>=0.4.7",
    "Pillow>=9.0.0",
]
