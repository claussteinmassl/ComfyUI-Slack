[pytest]
# Anchors pytest's rootdir here so it never imports the repo-root __init__.py
# (the ComfyUI entry point, whose relative imports only work inside ComfyUI).
# slack_resolve.py is loaded by file path in the test, so no package config is needed.
